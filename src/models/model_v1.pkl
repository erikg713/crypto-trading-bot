import pickle

with open("models/model_v1.pkl", "rb") as f:
    model = pickle.load(f)

# Example: predict
X_sample = [[0.5, 0.1, 0.2]]  # replace with actual feature vector
prediction = model.predict(X_sample)
print("Prediction:", prediction)

