import pandas as pd
import numpy as np
from datetime import datetime, timedelta
```
# Set random seed for reproducibility
np.random.seed(42)
```
# Generate synthetic BTC/USDT OHLCV data for 365 days
start_date = datetime.strptime("2024-07-01", "%Y-%m-%d")
days = 365
dates = [start_date + timedelta(days=i) for i in range(days)]
```
# Generate price data
base_price = 30000
open_prices = [base_price + np.random.normal(0, 500) for _ in range(days)]
high_prices = [open + np.random.uniform(100, 500) for open in open_prices]
low_prices = [open - np.random.uniform(100, 500) for open in open_prices]
close_prices = [np.random.uniform(low, high) for low, high in zip(low_prices, high_prices)]
volumes = np.random.uniform(100, 200, size=days)

# Create DataFrame
df = pd.DataFrame({
    "timestamp": [dt.strftime("%Y-%m-%d %H:%M:%S") for dt in dates],
    "open": np.round(open_prices, 2),
    "high": np.round(high_prices, 2),
    "low": np.round(low_prices, 2),
    "close": np.round(close_prices, 2),
    "volume": np.round(volumes, 2)
})

# Display a sample and save to file path
df.head(), df.to_csv("/mnt/data/historical_prices.csv", index=False)
